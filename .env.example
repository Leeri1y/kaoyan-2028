# Render 部署时无需手动设置，由 render.yaml 自动注入
PORT=3000
API_PUBLIC_URL=https://kaoyan-2028.onrender.com

# 简单的访问令牌，用于给写操作加一层门槛（详见 server.js 里的说明）。
# 需要在 Render 后台的环境变量里也设置同样的值，并把它填到
# public/js/config.js 的 window.APP_TOKEN 里，两边必须一致。
# 留空则不做任何校验（本地开发默认行为）。
APP_TOKEN=
